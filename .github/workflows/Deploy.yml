# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Deploy CI

on:
  push:
    branches: [ production ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: appleboy/ssh-action@v0.1.2
        with:
          host: ${{secrets.SSH_HOST}}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd /var/www/cosign.pro/
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S git pull
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S rsync -av --delete /var/www/cosign.pro/CollaborativeForms/node-server/ /var/www/cosign.pro/backend
            cd /var/www/cosign.pro/backend
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S touch /var/www/cosign.pro/backend/.env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo BUG_REPORT_EMAIL_ADDRESS=${{ secrets.BUG_REPORT_EMAIL_ADDRESS }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo CONTACT_EMAIL_ADDRESS=${{ secrets.CONTACT_EMAIL_ADDRESS }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo ENCRYPT_KEY=${{ secrets.ENCRYPT_KEY }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo JWT_PRIVATE_KEY=${{ secrets.JWT_PRIVATE_KEY }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo MAILER_LOGIN=${{ secrets.MAILER_LOGIN }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo MAILER_PASSWORD=${{ secrets.MAILER_PASSWORD }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo NODE_ENV=${{ secrets.NODE_ENV }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo S3_BUCKET_ACCESS_KEY=${{ secrets.S3_BUCKET_ACCESS_KEY }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo S3_BUCKET_NAME=${{ secrets.S3_BUCKET_NAME }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo S3_BUCKET_REGION=${{ secrets.S3_BUCKET_REGION }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo S3_BUCKET_SECRET_ACCESS_KEY=${{ secrets.S3_BUCKET_SECRET_ACCESS_KEY }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S npm install
            cd ..
            cd /var/www/cosign.pro/CollaborativeForms/client
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S npm install
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S touch .env.production
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S echo REACT_APP_BACKEND_ADDRESS=${{ secrets.REACT_APP_BACKEND_ADDRESS }} >> .env
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S npm run build
            cd ..
            cd ..
            echo ${{ secrets.SSH_PASSWORD }} | sudo -S rsync -av --delete /var/www/cosign.pro/CollaborativeForms/client/ /var/www/cosign.pro/html
            pm2 reload server